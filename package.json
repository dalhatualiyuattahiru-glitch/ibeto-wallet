{
  "name": "ibeto-wallet",
  "version": "1.0.0",
  "private": true,
  "description": "ibeto wallet app — static frontend + Netlify Functions backend",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
